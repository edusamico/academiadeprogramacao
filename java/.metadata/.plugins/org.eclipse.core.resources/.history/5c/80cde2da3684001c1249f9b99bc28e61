package main;

public class Printer<T> {
	private Iterable<T> iterable;
	
	public Printer (Iterable<T> it) {
		this.iterable = it;
	}
	
	public void printAll() {
		Iterator<T> myIterator = it.iterator();
		while (it.hasNext()) {
			System.out.println(it.next().toString());
		}
	}

}
